fun main(){

    var phy = 70
    var eng = 80

    var sum = (phy + eng)
    var remainder= eng % phy

    println(sum)
    println(remainder)

    var mult = eng * phy
    println(mult)

    var sub = eng - phy
    println(sub)

    var div = eng/phy
    println(div)
}